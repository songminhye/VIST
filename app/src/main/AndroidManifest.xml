<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jhl.ativ.vist">

    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false" />

    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />


    <application
        android:allowBackup="true"
        android:icon="@drawable/iconwithoutbackground"
        android:label="@string/app_name"
        android:roundIcon="@drawable/iconwithoutbackground"
        android:supportsRtl="true"
        android:noHistory="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".intro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TaggingActivity" />
        <activity android:name=".DataCheck" />
        <activity android:name=".flyActivity" />
        <activity android:name=".LastActivity" />
        <activity android:name=".LuggageArrivedActivity" />
        <activity android:name=".UserTaggingActivity" />
        <activity android:name=".TaggingPauseActivity"></activity>
    </application>

</manifest>